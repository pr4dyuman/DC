================================================================================
AgencyOS — COMPLETE DATABASE BUGS & ISSUES REPORT
Generated: 2026-03-15
================================================================================

CLUSTER OVERVIEW:
  📦 agency-os   — 27 collections, 7.16 MB  (PRODUCTION - mostly empty!)
  📦 test        — 15 collections, 2.15 MB  (OLD DATA - has real business data!)
  📦 sample_mflix — 6 collections, 123.43 MB (JUNK - MongoDB sample data)
  📦 local       — 1 collection, 39 GB      (System)
  📦 admin       — System DB

================================================================================
SECTION A: CRITICAL CLUSTER-LEVEL BUGS
================================================================================

BUG #1 [CRITICAL] — Real business data stuck in "test" database
  Database: test
  Issue: The "test" database contains ALL the real old business data:
    - 36 activities, 8 users, 7 projects, 5 tasks, 12 invoices,
      3 transactions, 3 clients, 7 services, 4 messages, 4 notifications
  BUT the app connects to "agency-os" database (via MONGODB_URI).
  This means ALL old data is invisible to the application.
  Root cause: Before multi-tenancy migration, the app was likely using
  the default "test" database (Mongoose default when no DB is specified
  in the URI). When the URI was updated to specify "agency-os", all old
  data was left behind.

BUG #2 [CRITICAL] — "agency-os" database is mostly empty
  Database: agency-os
  Issue: The production database only has:
    - 1 agency, 4 users, 5 notifications, 5 AI logs, 11 system logs,
      2 settings, 1 superadmin, 1 system settings
    - ALL business collections are EMPTY: projects (0), tasks (0),
      clients (0), invoices (0), transactions (0), services (0),
      assets (0), messages (0), leave requests (0), activities (0)
  Impact: The app appears to have no data for the business modules.

BUG #3 [CRITICAL] — "sample_mflix" database wastes 123 MB of storage
  Database: sample_mflix
  Issue: Contains 67,661 documents of MongoDB sample data:
    - 21,349 movies, 41,079 comments, 3,483 embedded movies,
      1,564 theaters, 185 users, 1 session
  Impact: Wasting cluster storage. Should be dropped entirely.

================================================================================
SECTION B: DATA INTEGRITY BUGS IN "test" DATABASE
================================================================================

BUG #4 [CRITICAL] — 3 tasks reference non-existent assignees
  Database: test → tasks
  Invalid assigneeIds: sc091wzrw, user-admin-1, user-emp-1
  Issue: These user IDs no longer exist in the users collection.
  Impact: 3 tasks cannot display their assignee. Likely from deleted
          users or leftover seed data.

BUG #5 [CRITICAL] — 5 projects reference non-existent clients
  Database: test → projects
  Affected projects:
    - "dc" (dym3dm7du) → clientId: "client-1" (deleted, was "Acme Corp")
    - "web dc" (d28kzjyad) → clientId: "client-1" (deleted, was "Acme Corp")
    - "sssp" (ggyzwm46n) → clientId: "eqpr429c2" (deleted, was "ankit")
    - "ssp" (f68ptrgbo) → clientId: "eqpr429c2" (deleted, was "ankit")
    - "Doctors Food Store Marketing" (xxi1xo4zh) → clientId: "oagnyapwj"
      (deleted, client name was "undefined" — stored as literal string!)
  Impact: These projects show broken client references.

BUG #6 [WARNING] — Agency usage counters are completely wrong (test DB)
  Database: test → agencies
  Agency "AgencyOS" (agency-1):
    - users:    tracked=3, actual=8 (off by 5)
    - projects: tracked=0, actual=7 (off by 7!!)
    - clients:  tracked=1, actual=3 (off by 2)
  Impact: Usage limits/billing calculations are wrong.

BUG #7 [WARNING] — 3 notifications for non-existent users (test DB)
  Database: test → notifications
  Invalid userIds: "client-1", "tvkfhftyf"
  Issue: "client-1" was a seed data client that was deleted.
         "tvkfhftyf" is a client ID, but notifications query the
         users collection (not clients collection).

BUG #8 [WARNING] — Orphaned settings doc with no agencyId (test DB)
  Database: test → settings
  Issue: 1 settings document has no agencyId (pre-migration leftover).

BUG #9 [INFO] — Transaction uses invalid category "Software"
  Database: test → transactions
  Issue: Two transactions have category: "Software", but the valid
         enum in the schema is: Project, Salary, Freelancer, Tax,
         Reimbursement, Retainer, Internal Transfer, Investor, Refund, Other.
  Impact: These transactions were saved before the enum was enforced.
         They would fail validation if updated.

================================================================================
SECTION C: DATA INTEGRITY BUGS IN "agency-os" DATABASE (PRODUCTION)
================================================================================

BUG #10 [CRITICAL] — Orphaned settings doc with no agencyId
  Database: agency-os → settings
  Issue: Doc (id: 699f168f8a77b104c671c55b) has NO agencyId.
         Created 2026-02-25 (pre-migration leftover).
         The correct settings doc (id: 69b5e7eabf42995502714c2b) has
         agencyId: "1fb52dc6-b038-408f-8e33-dda653fdf7b8".
  Fix: Delete the orphaned settings document.

BUG #11 [WARNING] — Admin user "chandan sharma" has empty password
  Database: agency-os → users
  User: b628f8f3-c728-4477-8650-3ceb7b1a55a6
  Issue: password field = "" (empty string).
  Impact: May not be able to log in, or could bypass validation.

BUG #12 [WARNING] — AI usage logs reference old "agency-1" ID
  Database: agency-os → aiusagelogs
  Issue: All 5 logs have agencyId: "agency-1" and userId: "admin-1t7bn519v"
         but the real agency uses UUID: "1fb52dc6-b038-408f-8e33-dda653fdf7b8".
  Impact: AI usage dashboard shows 0 usage for the real agency.

BUG #13 [INFO] — 4 of 5 AI usage logs have 0 tokens
  Database: agency-os → aiusagelogs
  Issue: inputTokens=0, outputTokens=0, totalTokens=0 for 4/5 entries.
  Impact: AI billing/usage tracking is inaccurate.

================================================================================
SECTION D: SCHEMA-LEVEL BUGS (Code Issues)
================================================================================

BUG #14 [CRITICAL] — Schema ↔ Type Mismatch: "favicon" missing from TS type
  File: lib/mongodb.ts line 236 vs lib/types.ts line 62-140
  Issue: Mongoose schema has favicon: String, but TypeScript Agency type
         does NOT include a favicon field.
  Impact: TypeScript won't see this field. Can't assign it type-safely.

BUG #15 [CRITICAL] — Schema ↔ Type Mismatch: planExpiresAt/planDuration not persisted
  File: lib/types.ts lines 77-78 vs lib/mongodb.ts lines 228-289
  Issue: TS type has planExpiresAt and planDuration, but Mongoose schema doesn't.
  Impact: These fields will NEVER be saved to MongoDB. Data is silently lost.

BUG #16 [WARNING] — Redundant "Settings" collection vs "Agency.settings"
  File: lib/mongodb.ts lines 577-585 + 188-198
  Issue: settings collection stores per-agency systemName, logo.
         Agency.settings also stores systemName. Dual source of truth.

BUG #17 [WARNING] — All dates stored as String instead of Date
  File: lib/mongodb.ts (throughout all schemas)
  Issue: createdAt, dueDate, timestamp, etc. are all { type: String }.
  Impact: Blocks efficient date queries, TTL indexes, aggregations.
  Exception: otps.expiresAt and ratelimits.expiresAt correctly use Date.

BUG #18 [WARNING] — No referential integrity enforcement
  File: lib/mongodb.ts (all FK fields)
  Issue: task.projectId, task.assigneeId, project.clientId are all
         bare strings with no validation.
  Impact: Deleting an entity leaves orphaned references (proven by bugs #4, #5).

BUG #19 [INFO] — User.role enum includes "client" but clients have own collection
  File: lib/mongodb.ts line 337

BUG #20 [INFO] — Invoice schema is very sparse (no line items, tax, invoice #)
  File: lib/mongodb.ts lines 444-451

BUG #21 [INFO] — Asset.size is String not Number (can't aggregate storage)
  File: lib/mongodb.ts line 532

BUG #22 [INFO] — Marketing Category slug has no duplicate handling
  File: models/marketing/Category.js lines 22-31

================================================================================
SUMMARY
================================================================================

TOTAL BUGS: 22

  🔴 CRITICAL: 7  (BUG #1, #2, #3, #4, #5, #10, #14, #15)
  🟡 WARNING:  8  (BUG #6, #7, #8, #11, #12, #16, #17, #18)
  🔵 INFO:     7  (BUG #9, #13, #19, #20, #21, #22)

RECOMMENDED ACTIONS (Priority Order):
  1. DELETE sample_mflix database entirely (save 123 MB)
  2. Decide: migrate test data to agency-os, or delete test DB
  3. Fix orphaned settings docs (delete pre-migration leftovers)
  4. Fix AI usage logs wrong agencyId (agency-1 → correct UUID)
  5. Fix admin user empty password
  6. Add favicon to Agency TypeScript type
  7. Add planExpiresAt/planDuration to Mongoose schema (or remove from type)
  8. Add cascade delete logic for all entity deletions
================================================================================
